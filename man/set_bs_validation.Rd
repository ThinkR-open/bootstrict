% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forms-layout.R
\name{set_bs_validation}
\alias{set_bs_validation}
\title{Set a control's validation state from the server}
\usage{
set_bs_validation(
  id,
  state = c("valid", "invalid", "none"),
  message = NULL,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{id}{Input id, as passed to the \verb{bs_*_input()} constructor.}

\item{state}{\code{"valid"}, \code{"invalid"}, or \code{"none"} to clear the state.}

\item{message}{Optional replacement text for the feedback message. Plain
text: it is set with \code{textContent}.}

\item{session}{The Shiny session.}
}
\value{
Nothing, called for its side effect.
}
\description{
Toggles \code{.is-valid} / \code{.is-invalid} on the control registered under \code{id} and,
when \code{message} is supplied, replaces the text of the matching feedback
message declared with \code{\link[=bs_feedback]{bs_feedback()}}.
}
\examples{
if (interactive()) {
  set_bs_validation("user", "invalid", "That name is taken.")
}
}
\seealso{
\code{\link[=bs_feedback]{bs_feedback()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{parse_scss_variables}
\alias{parse_scss_variables}
\title{Parse a SASS/SCSS variable sheet into a named list}
\usage{
parse_scss_variables(path)
}
\arguments{
\item{path}{Path to a \code{.scss} file (SCSS syntax, \verb{$name: value;} — the
indented \code{.sass} syntax has no semicolons and cannot be parsed).}
}
\value{
A named list of Sass variable values. Names use the Bootstrap
convention without the leading \code{$} (e.g. \code{primary}, \code{font-family-base}).
}
\description{
Reads a \verb{_variables.scss} style file (the kind a designer exports) and
extracts top-level \verb{$name: value;} declarations into a named list suitable
for passing to \code{\link[=bootstrict_theme]{bootstrict_theme()}}. Trailing
\code{!default} / \code{!global} flags and line/block comments are stripped. Values
are returned verbatim as strings (Sass resolves them at compile time), so
maps, functions and colour expressions all pass straight through.
}
\details{
The file is scanned rather than read line by line, so a declaration may
span as many lines as it likes (\verb{$theme-colors}, \verb{$grid-breakpoints} and
\verb{$spacers} always do), and a \verb{;}, \verb{//} or \verb{/* */} inside a quoted string or
an unquoted \code{url()} is read as data. Anything that is not a top-level
variable declaration — \verb{@use}/\verb{@import}, a rule block — is ignored. A final
declaration with no trailing \verb{;} is still read.
}
\examples{
tmp <- tempfile(fileext = ".scss")
writeLines(c("$primary: #ff6600;", "$border-radius: 0.5rem !default;"), tmp)
parse_scss_variables(tmp)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modal.R
\name{bs_modal}
\alias{bs_modal}
\alias{bs_modal_header}
\alias{bs_modal_title}
\alias{bs_modal_body}
\alias{bs_modal_footer}
\title{Bootstrap modal}
\usage{
bs_modal(
  id,
  ...,
  title = NULL,
  footer = NULL,
  size = NULL,
  centered = FALSE,
  scrollable = FALSE,
  fullscreen = FALSE,
  backdrop = TRUE,
  keyboard = TRUE,
  class = NULL
)

bs_modal_header(..., class = NULL)

bs_modal_title(..., level = 1, class = NULL)

bs_modal_body(..., class = NULL)

bs_modal_footer(..., class = NULL)
}
\arguments{
\item{id}{Modal id; open state available as \code{input$id}.}

\item{...}{Modal body content and named HTML attributes applied to the root.}

\item{title}{Optional header title. When supplied, a \code{.modal-header} with a
title and a close button is rendered.}

\item{footer}{Optional footer content (e.g. buttons), placed in a
\code{.modal-footer}.}

\item{size}{Dialog size: \code{"sm"}, \code{"lg"} or \code{"xl"}.}

\item{centered}{If \code{TRUE}, vertically centre the dialog
(\code{.modal-dialog-centered}).}

\item{scrollable}{If \code{TRUE}, scroll long bodies inside the dialog
(\code{.modal-dialog-scrollable}).}

\item{fullscreen}{\code{TRUE} for an always-fullscreen modal, or a breakpoint
(\code{"sm"}/\code{"md"}/\code{"lg"}/\code{"xl"}/\code{"xxl"}) for fullscreen below that breakpoint
(\code{.modal-fullscreen-{bp}-down}).}

\item{backdrop}{Backdrop behaviour: \code{TRUE} (backdrop shown, dismiss on
outside click), \code{"static"} (backdrop shown, do not dismiss on outside
click) or \code{FALSE} (no backdrop at all).}

\item{keyboard}{If \code{FALSE}, the modal cannot be closed with the Escape key.}

\item{class}{Extra classes.}

\item{level}{Heading level (1-6) for the modal title.}
}
\value{
A modal tag.
}
\description{
A faithful Bootstrap 5 modal dialog. The modal's open/closed state is
reported to the server as \code{input$id} (\code{TRUE} when shown), and can be driven
server-side with \code{\link[=show_bs_modal]{show_bs_modal()}}, \code{\link[=hide_bs_modal]{hide_bs_modal()}} and \code{\link[=toggle_bs_modal]{toggle_bs_modal()}}.
}
\details{
For full control over the dialog structure (in place of the \code{title} and
\code{footer} shortcuts) compose the body yourself with \code{\link[=bs_modal_header]{bs_modal_header()}},
\code{\link[=bs_modal_body]{bs_modal_body()}} and \code{\link[=bs_modal_footer]{bs_modal_footer()}}.
}
\examples{
bs_modal("info", "Modal body text.", title = "Heads up")
}

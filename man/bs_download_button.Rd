% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{bs_download_button}
\alias{bs_download_button}
\title{Bootstrap download button}
\usage{
bs_download_button(
  id,
  label = "Download",
  ...,
  icon = NULL,
  color = "primary",
  outline = FALSE,
  size = NULL,
  class = NULL
)
}
\arguments{
\item{id}{Output id, matching the \code{\link[shiny:downloadHandler]{shiny::downloadHandler()}} assigned to
\code{output$id}.}

\item{label}{Button label (text or tags).}

\item{...}{Additional content and named HTML attributes, forwarded to the
anchor.}

\item{icon}{Optional icon tag placed before the label. \code{NULL} (the default)
renders no icon.}

\item{color}{One of the eight Bootstrap theme colours, or \code{"link"}.}

\item{outline}{If \code{TRUE}, an outline button (\verb{.btn-outline-*}).}

\item{size}{\code{"sm"} or \code{"lg"}.}

\item{class}{Extra classes.}
}
\value{
An anchor tag styled as a Bootstrap button.
}
\description{
Delegates to \code{\link[shiny:downloadButton]{shiny::downloadButton()}} and adapts its markup to Bootstrap 5.
}
\details{
shiny hardcodes \code{class = "btn btn-default shiny-download-link"}.
\code{.btn-default} is a Bootstrap 3 class with no Bootstrap 5 equivalent, so the
button renders with the \code{.btn} box but no colour, border or hover state. We
drop it and substitute a real 5.3 variant (\code{.btn-primary},
\code{.btn-outline-secondary}, ...), matching \code{\link[=bs_button]{bs_button()}}'s API.

Everything else is left to shiny: the \code{shiny-download-link} class, the
\code{href} / \code{target} / \code{download} attributes and the auto-enable dance (the
anchor ships \code{.disabled} + \code{aria-disabled} and shiny's client enables it once
the matching \code{\link[shiny:downloadHandler]{shiny::downloadHandler()}} is registered) are untouched, so
\code{output$id <- downloadHandler(...)} works exactly as usual.

Unlike shiny, \code{icon} defaults to \code{NULL}: shiny's default is a Font Awesome
icon, which is outside Bootstrap 5 and therefore outside this package.
}
\examples{
bs_download_button("report", "Download CSV")
bs_download_button("report", "Export", color = "secondary", outline = TRUE)
}
\seealso{
\code{\link[shiny:downloadHandler]{shiny::downloadHandler()}}, \code{\link[=bs_download_link]{bs_download_link()}}, \code{\link[=bs_button]{bs_button()}}
}

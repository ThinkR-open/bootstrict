% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/navbar.R
\name{bs_navbar}
\alias{bs_navbar}
\alias{bs_navbar_brand}
\alias{bs_navbar_nav}
\alias{bs_navbar_text}
\title{Bootstrap navbar}
\usage{
bs_navbar(
  ...,
  brand = NULL,
  id = NULL,
  expand = "lg",
  bg = NULL,
  theme = NULL,
  placement = NULL,
  fluid = TRUE,
  class = NULL
)

bs_navbar_brand(..., href = "#", class = NULL)

bs_navbar_nav(..., scroll = FALSE, scroll_height = NULL, class = NULL)

bs_navbar_text(..., class = NULL)
}
\arguments{
\item{...}{Navbar content (brand, nav lists, text, ...) placed inside the
collapsible container, plus named HTML attributes for the \verb{<nav>}.}

\item{brand}{Optional brand element, typically \code{\link[=bs_navbar_brand]{bs_navbar_brand()}}, rendered
before the toggler so it stays visible when collapsed.}

\item{id}{Navbar id; used to wire the toggler to its collapsible region
(\code{"<id>-collapse"}). Defaults to a unique auto-generated id so multiple
navbars on a page never collide.}

\item{expand}{Breakpoint at which the navbar expands: one of
\code{"sm"}, \code{"md"}, \code{"lg"}, \code{"xl"}, \code{"xxl"}, or \code{TRUE} to always expand
(\code{.navbar-expand}). Use \code{FALSE}/\code{NULL} to never expand (always collapsed).}

\item{bg}{Background colour (\verb{.bg-*}): one of the Bootstrap theme colours,
or \code{"body"}, \code{"body-secondary"}, \code{"body-tertiary"} (the Bootstrap 5.3
navbar default), \code{"white"}, \code{"black"}, \code{"transparent"}.}

\item{theme}{Colour scheme applied via \code{data-bs-theme} (the Bootstrap 5.3
colour-modes idiom; \code{.navbar-light}/\code{.navbar-dark} are deprecated):
\code{"light"} or \code{"dark"}.}

\item{placement}{Fixed/sticky placement: one of \code{"fixed-top"},
\code{"fixed-bottom"}, \code{"sticky-top"}, \code{"sticky-bottom"}.}

\item{fluid}{If \code{TRUE} (default) use a full-width \code{.container-fluid},
otherwise a fixed-width \code{.container}.}

\item{class}{Extra classes for the \verb{<nav>}.}

\item{href}{Link target for the brand.}

\item{scroll}{If \code{TRUE}, enable vertical scrolling within the collapsed
navbar nav (\code{.navbar-nav-scroll}, Bootstrap 5.1).}

\item{scroll_height}{Max scroll height (sets \code{--bs-scroll-height}), e.g.
\code{"75vh"} or \code{"200px"}. Only applies when \code{scroll = TRUE}.}
}
\value{
A navbar tag.
}
\description{
A responsive navigation header. Compose with \code{\link[=bs_navbar_brand]{bs_navbar_brand()}},
\code{\link[=bs_navbar_nav]{bs_navbar_nav()}} (containing \code{bs_nav_item()} / \code{bs_nav_link()} from the
nav-tabs group), \code{\link[=bs_navbar_text]{bs_navbar_text()}} and, optionally, \code{bs_dropdown()}. The
navbar collapses behind a toggler below the \code{expand} breakpoint.
}
\examples{
bs_navbar(brand = bs_navbar_brand("Navbar"), bg = "light", theme = "light")
bs_navbar_brand("Acme", href = "/")
bs_navbar_nav(bs_nav_item(bs_nav_link("Home", active = TRUE)))
bs_navbar_text("Signed in as Mark Otto")
}

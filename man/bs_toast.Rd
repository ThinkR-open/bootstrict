% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toast.R
\name{bs_toast}
\alias{bs_toast}
\title{Bootstrap toast}
\usage{
bs_toast(
  id,
  ...,
  title = NULL,
  icon = NULL,
  autohide = TRUE,
  delay = 5000,
  animation = TRUE,
  class = NULL
)
}
\arguments{
\item{id}{Toast id; shown state available as \code{input$id}.}

\item{...}{Toast body content and named HTML attributes applied to the root.}

\item{title}{Optional header title. When supplied, a \code{.toast-header} with the
title and a close button is rendered.}

\item{icon}{Optional icon placed before the title in the header.}

\item{autohide}{If \code{TRUE} (default), hide the toast automatically after
\code{delay} milliseconds.}

\item{delay}{Delay in milliseconds before auto-hiding (when \code{autohide}).}

\item{animation}{If \code{FALSE}, disable the fade animation.}

\item{class}{Extra classes.}
}
\value{
A toast tag.
}
\description{
A lightweight, dismissible notification. The toast's visibility is reported
to the server as \code{input$id} (\code{TRUE} when shown), and can be driven
server-side with \code{\link[=show_bs_toast]{show_bs_toast()}} / \code{\link[=hide_bs_toast]{hide_bs_toast()}}. Place one or more
toasts inside a \code{\link[=bs_toast_container]{bs_toast_container()}} to position them on screen, or push
transient notifications entirely from the server with \code{\link[=bs_notify_toast]{bs_notify_toast()}}.
}
\examples{
bs_toast("hello", "Hello, world!", title = "Bootstrict")
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/content.R
\name{bs_table}
\alias{bs_table}
\title{Bootstrap table}
\usage{
bs_table(
  data = NULL,
  ...,
  striped = FALSE,
  bordered = FALSE,
  borderless = FALSE,
  hover = FALSE,
  small = FALSE,
  variant = NULL,
  responsive = FALSE,
  align = NULL,
  caption = NULL,
  class = NULL
)
}
\arguments{
\item{data}{A data frame or matrix to render. When \code{NULL}, the unnamed
\code{...} arguments are used as the table's children instead.}

\item{...}{Manual table children (when \code{data} is \code{NULL}) and named HTML
attributes applied to the \verb{<table>} element.}

\item{striped}{Add zebra-striping to table rows (\code{.table-striped}).}

\item{bordered}{Add borders on all sides (\code{.table-bordered}).}

\item{borderless}{Remove all borders (\code{.table-borderless}).}

\item{hover}{Enable a hover state on rows (\code{.table-hover}).}

\item{small}{Make the table more compact (\code{.table-sm}).}

\item{variant}{Theme colour for the whole table (\verb{.table-*}), one of the
Bootstrap theme colours.}

\item{responsive}{Make the table scroll horizontally on small devices. Use
\code{TRUE} for \code{.table-responsive}, or a breakpoint string (\code{"sm"}, \code{"md"},
\code{"lg"}, \code{"xl"}, \code{"xxl"}) for \code{.table-responsive-{bp}}.}

\item{align}{Vertical alignment of cells (\verb{.align-*}), e.g. \code{"middle"},
\code{"top"}, \code{"bottom"}.}

\item{caption}{Optional table caption text rendered in a \verb{<caption>}.}

\item{class}{Extra classes.}
}
\value{
A table tag (wrapped in a responsive container when \code{responsive} is
set).
}
\description{
Render a faithful Bootstrap 5 \verb{<table class="table">}. Pass a data frame or
matrix in \code{data} to have the header and body built automatically from its
column names and rows; otherwise supply \verb{<thead>}/\verb{<tbody>} markup (or any
table children) through \code{...}.
}
\examples{
bs_table(head(mtcars), striped = TRUE, hover = TRUE)
}

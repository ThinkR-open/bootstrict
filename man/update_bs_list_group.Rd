% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list-group.R
\name{update_bs_list_group}
\alias{update_bs_list_group}
\title{Control a list group selection from the server}
\usage{
update_bs_list_group(
  id,
  selected = NULL,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{id}{List group id (namespaced automatically inside modules).}

\item{selected}{Item value to activate. \code{NULL} (the default) leaves the
current selection unchanged; \code{character(0)} clears it (deselects every
item and resets \code{input$id} to \code{NULL}).}

\item{session}{The Shiny session.}
}
\value{
Invisibly \code{NULL}, called for its side effect.
}
\description{
Activates the item whose \code{value} matches \code{selected} in a selectable
\code{\link[=bs_list_group]{bs_list_group()}} (one created with an \code{id}).
}
\examples{
\dontrun{
update_bs_list_group("my_group", selected = "two")
update_bs_list_group("my_group", selected = character(0)) # clear
}
}

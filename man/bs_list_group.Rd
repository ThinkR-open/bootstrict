% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list-group.R
\name{bs_list_group}
\alias{bs_list_group}
\alias{bs_list_group_item}
\title{Bootstrap list group}
\usage{
bs_list_group(
  id = NULL,
  ...,
  flush = FALSE,
  numbered = FALSE,
  horizontal = FALSE,
  class = NULL
)

bs_list_group_item(
  ...,
  value = NULL,
  active = FALSE,
  disabled = FALSE,
  color = NULL,
  action = FALSE,
  href = NULL,
  class = NULL
)
}
\arguments{
\item{id}{Optional list group id. When set, the active item's value is
reported as \code{input$id} and the group is wired for selection.}

\item{...}{List items built with \code{\link[=bs_list_group_item]{bs_list_group_item()}}, plus named HTML
attributes.}

\item{flush}{If \code{TRUE}, render edge-to-edge without an outer border and
rounded corners (\code{.list-group-flush}).}

\item{numbered}{If \code{TRUE}, render a numbered list (\verb{<ol class="list-group list-group-numbered">}).}

\item{horizontal}{Lay items out horizontally. \code{TRUE} for
\code{.list-group-horizontal}, or a breakpoint string (\code{"sm"}, \code{"md"}, \code{"lg"},
\code{"xl"}, \code{"xxl"}) for \code{.list-group-horizontal-{bp}}.}

\item{class}{Extra classes.}

\item{value}{Item value reported to the server when selected (its
\code{data-value}). Only meaningful in a selectable group (when the parent
\code{\link[=bs_list_group]{bs_list_group()}} has an \code{id}).}

\item{active}{If \code{TRUE}, mark the item as the active/selected one.}

\item{disabled}{If \code{TRUE}, mark the item disabled.}

\item{color}{Contextual theme colour (\verb{.list-group-item-*}).}

\item{action}{If \code{TRUE}, render an actionable \verb{<button>} item
(\code{.list-group-item-action}). Ignored when \code{href} is supplied (which always
renders an actionable \verb{<a>}).}

\item{href}{Link target; renders the item as an \verb{<a>} (always actionable).}
}
\value{
A list group tag.
}
\description{
A flexible container for displaying a series of content. Compose with
\code{\link[=bs_list_group_item]{bs_list_group_item()}}. When \code{id} is supplied the group becomes selectable:
clicking an action item (\code{action = TRUE} or \code{href}) reports that item's
\code{value} (its \code{data-value}) as \code{input$id}, and the selection can be driven
server-side with \code{\link[=update_bs_list_group]{update_bs_list_group()}}.
}
\examples{
bs_list_group(
  bs_list_group_item("An item"),
  bs_list_group_item("A second item", active = TRUE)
)
bs_list_group_item("A link item", href = "#", value = "a")
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forms-layout.R
\name{bs_feedback}
\alias{bs_feedback}
\title{Attach validation feedback to a form control}
\usage{
bs_feedback(input, valid = NULL, invalid = NULL, state = NULL)
}
\arguments{
\item{input}{A control tag tree produced by a \verb{bs_*_input()} constructor.}

\item{valid}{Message shown when the control is valid, or \code{NULL} for none.}

\item{invalid}{Message shown when the control is invalid, or \code{NULL}.}

\item{state}{Initial state: \code{"valid"}, \code{"invalid"} or \code{NULL} (neutral).}
}
\value{
The input, with the feedback messages attached.
}
\description{
Bootstrap only shows a \code{.valid-feedback} / \code{.invalid-feedback} message when
it is a \emph{following sibling} of the control carrying \code{.is-valid} /
\code{.is-invalid}. A feedback div placed after a \verb{bs_*_input()} is a sibling of
shiny's input container, not of the control inside it, so it would never
display. \code{bs_feedback()} inserts the messages in the right place, whatever
the control's internal structure, and is the supported way to validate a
bootstrict input.
}
\details{
Declare the messages in the UI and switch the state from the server with
\code{\link[=set_bs_validation]{set_bs_validation()}}.
}
\examples{
bs_feedback(
  bs_text_input("user", "Username"),
  invalid = "Please choose a username."
)
}
\seealso{
\code{\link[=set_bs_validation]{set_bs_validation()}}, \code{\link[=bs_valid_feedback]{bs_valid_feedback()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/offcanvas.R
\name{bs_offcanvas}
\alias{bs_offcanvas}
\title{Bootstrap offcanvas}
\usage{
bs_offcanvas(
  id,
  ...,
  title = NULL,
  placement = "start",
  backdrop = TRUE,
  scroll = FALSE,
  responsive = NULL,
  class = NULL
)
}
\arguments{
\item{id}{Offcanvas id; open state available as \code{input$id}.}

\item{...}{Offcanvas body content and named HTML attributes applied to the
root.}

\item{title}{Optional header title. When supplied, an \code{.offcanvas-header}
with a title and a close button is rendered.}

\item{placement}{Edge the panel slides in from: \code{"start"} (left), \code{"end"}
(right), \code{"top"} or \code{"bottom"}.}

\item{backdrop}{Backdrop behaviour: \code{TRUE} (default, dismiss on outside
click), \code{FALSE} (no backdrop) or \code{"static"} (backdrop that does not dismiss
on outside click).}

\item{scroll}{If \code{TRUE}, allow body scrolling while the offcanvas is open.}

\item{responsive}{Optional breakpoint (\code{"sm"}, \code{"md"}, \code{"lg"}, \code{"xl"},
\code{"xxl"}). Below it the panel behaves as an offcanvas; at or above it the
content is shown inline (Bootstrap 5.2 responsive offcanvas).}

\item{class}{Extra classes.}
}
\value{
An offcanvas tag.
}
\description{
A hidden sidebar panel that slides in from an edge of the viewport. The
open/closed state is reported to the server as \code{input$id} (\code{TRUE} when
shown), and can be driven server-side with \code{\link[=show_bs_offcanvas]{show_bs_offcanvas()}},
\code{\link[=hide_bs_offcanvas]{hide_bs_offcanvas()}} and \code{\link[=toggle_bs_offcanvas]{toggle_bs_offcanvas()}}. Open it declaratively from
the UI with \code{\link[=bs_offcanvas_trigger]{bs_offcanvas_trigger()}}.
}
\examples{
bs_offcanvas("menu", "Sidebar content.", title = "Menu")
bs_offcanvas("nav", "Shown inline on lg+.", responsive = "lg")
}

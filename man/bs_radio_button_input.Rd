% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forms-controls.R
\name{bs_radio_button_input}
\alias{bs_radio_button_input}
\alias{bs_checkbox_button_input}
\title{Bootstrap toggle button groups}
\usage{
bs_radio_button_input(
  id,
  label = NULL,
  choices,
  selected = NULL,
  ...,
  color = "primary",
  outline = TRUE,
  size = NULL,
  vertical = FALSE,
  class = NULL
)

bs_checkbox_button_input(
  id,
  label = NULL,
  choices,
  selected = NULL,
  ...,
  color = "primary",
  outline = TRUE,
  size = NULL,
  vertical = FALSE,
  class = NULL
)
}
\arguments{
\item{id}{Input id; the selection is available as \code{input$id} (a single
string for radio buttons, a character vector — possibly empty — for
checkboxes).}

\item{label}{Label shown above the group, or \code{NULL} for none.}

\item{choices}{Character vector of values. Names, when present, are used as
the button labels.}

\item{selected}{Initially selected value(s). Defaults to the first choice
for radio buttons and to none for checkboxes.}

\item{...}{Named HTML attributes applied to the \code{.btn-group}.}

\item{color}{Button theme colour.}

\item{outline}{If \code{TRUE} (the default, as in the Bootstrap examples),
outline buttons (\verb{.btn-outline-*}).}

\item{size}{Button size: \code{"sm"} or \code{"lg"}.}

\item{vertical}{If \code{TRUE}, stack the buttons (\code{.btn-group-vertical}).}

\item{class}{Extra classes for the \code{.btn-group}.}
}
\value{
A form control tag.
}
\description{
The segmented control from the Bootstrap "Button group" page: a row of
buttons backed by hidden radio or checkbox inputs (\code{.btn-check}).
\code{bs_radio_button_input()} picks one value, \code{bs_checkbox_button_input()} picks
any number.
}
\details{
These are native controls, not restyled shiny inputs, so drive them with
\code{\link[=update_bs_toggle_buttons]{update_bs_toggle_buttons()}} rather than \code{shiny::updateRadioButtons()}.
}
\examples{
bs_radio_button_input("size", "Size", c(Small = "s", Large = "l"))
bs_checkbox_button_input("opts", "Options", c("a", "b"))
}
\seealso{
\code{\link[=update_bs_toggle_buttons]{update_bs_toggle_buttons()}}, \code{\link[=bs_button_group]{bs_button_group()}}
}
